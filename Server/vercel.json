{
  "version": 2,
  "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/success", "dest": "/success" },
    { "src": "/cancel", "dest": "/cancel" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
